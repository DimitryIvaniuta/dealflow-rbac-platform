# dealflow-rbac-platform
Spring Boot (Java) platform with PostgreSQL/Flyway (separate migrations per table), JPA + Specifications filtering, GraphQL SPQR annotation-driven schema generation, OAuth2 Resource Server (JWT), and workspace-scoped RBAC enforced via method security
